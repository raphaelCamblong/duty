---
title: duty
description: A little task system that lives in your repo as plain markdown files.
template: splash
hero:
  title: duty
  tagline: A little task system that lives in your repo as plain markdown files. No app, no database, no account. Built for working with your coding agent.
  image:
    file: ../../assets/duty-tui.png
    alt: The duty TUI showing a board of tasks and their statuses.
  actions:
    - text: Get started
      link: /getting-started/
      icon: right-arrow
    - text: GitHub
      link: https://github.com/raphaelCamblong/duty
      icon: github
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

<CardGrid stagger>
	<Card title="Markdown all the way down" icon="document">
		One file is one task. A `BOARD.md` keeps the order. You can grep, diff, and
		commit all of it — that's the whole model.
	</Card>
	<Card title="One binary: CLI + TUI" icon="laptop">
		The CLI does the writing. The TUI just watches the files — keep `duty tui`
		open and see every status change land in real time.
	</Card>
	<Card title="Tracks that nest" icon="seti:folder">
		A track is just a folder, and folders nest. Any folder with a `BOARD.md` is
		a board — no registration, no config.
	</Card>
</CardGrid>
